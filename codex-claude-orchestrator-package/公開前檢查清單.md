# 公開前檢查清單

這份套件已針對公開上傳做過整理，但在上 GitHub public 前，仍建議再確認以下事項：

## 已排除的內容

- 舊的 orchestrator 歷史執行輸出
- `.pytest_cache`
- `__pycache__`
- 與本次主題無關的下載資料夾
- 其他舊 deliverables

## 目前保留但屬於「測試用途」的字串

以下字串不是實際憑證，而是測試 placeholder：

- `local-test-key`
- `dummy-key`
- `ollama`

這些字串出現在：

- `router-config.json`
- `run-claude.sh`
- `start-ccr.sh`
- `run_codex_guarded.sh`
- `run_codex_openai_compat.sh`
- `docker-compose.yml`
- `README.md`

## 公開建議

1. 若要上 public repo，建議只上傳這份整理後的套件資料夾，不要上傳整個 `claude_goal` workspace。
2. 如果要再更保守，可把 placeholder 字串統一改成：
   - `CHANGE_ME`
   - `PLACEHOLDER_ONLY`
3. 若後續加入真實 API key、帳密、公司 URL、內網 IP，請不要提交。

## 推薦公開範圍

推薦只公開：

- `ubuntu22-claude-ccr/`
- `使用說明.md`
- `驗證摘要.txt`
- `公開前檢查清單.md`
